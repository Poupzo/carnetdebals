import { async, ComponentFixture, TestBed } from '@angular/core/testing';
/*import { TypeChampionnatComponent } from './type-championnat.component';

describe('TypeChampionnatComponent', () => {
  let component: TypeChampionnatComponent;
  let fixture: ComponentFixture<TypeChampionnatComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TypeChampionnatComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TypeChampionnatComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
